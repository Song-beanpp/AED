# Belgium AED Optimization — Environment Variables
# Copy this file as .env and modify as needed.

# Random seed for reproducibility (default: 42)
AED_SEED=42

# PyTorch device override (auto-detects CUDA/MPS if unset)
# Options: cpu, cuda, mps
# TORCH_DEVICE=cpu
